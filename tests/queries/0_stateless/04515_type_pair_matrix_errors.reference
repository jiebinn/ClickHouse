-- comparison overflow during scale conversion
-- the same comparisons with the overflow check disabled
0
0
0
-- extreme values that must compare without overflow
1
1
-- division by zero in every width direction
