first query (populates the cache), result:
0
second query (uses the cache), result:
0
second query without the cache, result:
0
rows read by the second query with the cache (expect 0 - whole file skipped):
0
second query without the cache read more than zero rows (expect 1):
1
matching predicate, run 1 (expect 1):
1
matching predicate, run 2 (expect 1):
1
